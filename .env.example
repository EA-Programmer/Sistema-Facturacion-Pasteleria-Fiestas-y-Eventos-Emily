DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
AUTH_SECRET="replace-with-a-secure-random-secret"
# Optional. If empty, AUTH_SECRET is used to encrypt the local SRI signature password.
SIGNATURE_ENCRYPTION_SECRET="replace-with-a-different-secure-random-secret"
ADMIN_EMAIL="admin@emily.local"
ADMIN_PASSWORD="admin"
RESEND_API_KEY="re_..."
EMAIL_FROM="Facturacion Pasteleria <facturas@tudominio.com>"
